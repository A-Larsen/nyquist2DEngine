-- _ui = require("modules/ui_mt")

-- local ui = {}

-- function ui:init(spritesheet, window_size, options)
--     setmetatable(ui, _ui)
--     ui:_init(spritesheet, window_size, "NotoSansMono-Regular.ttf", 12, options)
-- end


-- function ui:update()
--     nyq.images.update(ui.start_menu.id, ui.start_menu.position)
-- end

-- return ui
